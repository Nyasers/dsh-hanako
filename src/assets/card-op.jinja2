<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>DSH 任务</title>
{{= it.hcLink }}
<style>{{= it.assets.css }}</style>
</head>
<body data-hana-theme="{{= it.esc(it.th) }}">
<div id="dsh-root" data-op="{{= it.esc(it.opId) }}" data-session="{{= it.esc(it.sessionId) }}" data-rpc="{{= it.esc(it.rpcId) }}" data-timeout="{{= it.esc(it.timeoutMs) }}"></div>
<script>window.__API="{{= it.base }}";</script>
<script>{{= it.assets.js }}</script>
</body>
</html>