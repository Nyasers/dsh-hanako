- **v1.0.1-alpha.1**（unreleased）：DSH WebUI sidebar 进宿主侧栏桥——主页面隐藏 sidebar + embedUrl 转发到 Hana functionPanel（feat/sidebar-fp）。
① **新 cordis 插件 @dsh-hanako/sidebar**（dsh-plugin/@dsh-hanako/sidebar/，经 dsh-host-webserver `tapIndex` 注入自包含脚本；幂等：检测已有标记跳过）：按 `location.search` 的 `dshana` 参数分支——
   · **?dshana=sidebar**（宿主功能面板 iframe，宽 180-400px 必窄屏 → dsh sidebar 折叠成 56px rail）：等待 AppFrame 挂载（轮询 ≤30s 静默上限）→ 定位 sidebar 列（任务契约 `div[data-slot="sidebar"]` 优先，兜底 frame 语义属性 `data-sidebar-collapsed`/`data-details-collapsed` 与结构扫描 inline grid 三列）→ 折叠态时模拟点击展开 toggle（aria-label toggle.open/collapse 语义，class 含 toggle / logoRow 结构兜底；narrow 下 toggleSidebar 翻转 narrowExpanded，宿主侧栏宽度固定 → narrow 恒定 → 展开状态钉住）→ CSS !important 覆盖 frame `grid-template-columns` 为单轨道（minmax(0,1fr) 0 0）+ display:none 隐藏 center/details 等非 sidebar 元素（保留 [data-shell-overlay] 浮层宿主）→ sidebar 占满 iframe；MutationObserver（attributeFilter style）在 React 重渲染后重放覆盖，details 宽度变化保持响应式。
   · **?dshana=main**（主页面卡片 iframe）：隐藏 sidebar 列（display:none + grid 首轨道 0 !important），center 占满；details 保留并跟随 React 宽度。
   · **无参数**：零动作，正常 dsh UI 行为不变（顶层浏览器直开 3080 不受影响）。
   注入脚本自包含/幂等/容错（AppFrame 未渲染或找不到元素静默重试有上限，不报错不阻断）；样式不硬编码颜色（沿用 dsh 主题 class/变量），宽度覆盖用元素级 style.setProperty(..., "important") 压过 React inline。
② **manifest.json**：functionPanel 加 `embedUrl: "http://127.0.0.1:3080/?dshana=sidebar"`（v2 卡片 functionPanel.embedUrl 字段，loopback 校验合法；保留现有 id/label）。
③ **主页面 iframe src 带 ?dshana=main**：webui-shell.jinja2 attach() 的 base（`http://127.0.0.1:<port>/?dshana=main`，dshReload bust 改 `&dshReload=`）+ webui.js buildShell 首帧 iframe src（`/?dshana=main`）；其余行为不变（就绪事件化 / 主题桥 / 剪贴板桥 / hana 面板桥全保留）。
④ **注册链路**：dsh-plugin/dsh-hanako.patch.yml 加 insert（@dsh-hanako/sidebar）；src/migrate.js junction-converge 包列表加 sidebar（profiles/node_modules junction，六→七）。
⑤ **分离后同步桥**（同一注入脚本内扩展）：两个 dsh UI iframe（?dshana=sidebar 宿主侧栏 / ?dshana=main 主页面）是兄弟 iframe、同源（127.0.0.1:3080）但前端 store 无跨实例同步（dsh-client-store persist 机械写 localStorage，无 storage 事件/BroadcastChannel；current session 是前端 selection 状态）→ 自建 **localStorage + storage 事件**通道（key `dshana.sync`，value JSON `{ action, title?, dupIndex?, absIndex?, ts }`，ts 过期过滤）：
   · **侧栏捕获**（sidebar 模式）：document capture 阶段监听 click，识别三类交互——点会话行 → open-session（标题 + 同标题重复索引 + 绝对索引，不 stopPropagation：侧栏自身切高亮，两 store 指向同一 session 无害）；New Session 按钮（aria-label 新建会话/New session）→ new-session（stopPropagation 阻断侧栏自身创建，避免双会话）；设置入口（button[aria-haspopup="dialog"]）→ open-settings（stopPropagation 阻断侧栏自身开面板）。行内菜单按钮（会话/工作区操作 aria-label）与展开 toggle 点击不触发同步。
   · **主区执行**（main 模式）：window storage 事件（同源其他文档触发，自身不触发 → 天然无自环）→ 解析指令 → 在主区隐藏但 mounted 的 sidebar DOM 里模拟点击（.click() 对 display:none 元素仍冒泡到 React root 事件委托 → React onClick 正常触发）→ dsh 前端切换 current session → 主区 center 落实显示；找不到目标（列表未加载/结构变化）→ 500ms×10 有上限重试后静默放弃。
   · **会话行匹配**：会话行无稳定 id 属性（sessionId 只在 React 闭包）→ 标题为主匹配（从行内菜单按钮 aria-label 解析：zh 会话“{title}”的操作 / en Session actions for {title}；空白会话行无菜单按钮 → 本地化 blank 标题“新会话”/“New Session”），同标题重复按 dupIndex，找不到回落 absIndex（两 iframe 同后端同列表序）。会话行 DOM 特征：`[role="treeitem"][aria-selected]`（工作区行为 `[role="treeitem"][aria-expanded]`，可区分）；树容器 `[role="tree"][aria-label="会话"/"Sessions"]`。
   · **main 模式强制展开**：run() 统一折叠态先展开（sidebar 模式本来要展开占满；main 模式保证隐藏 sidebar 内会话列表 mounted——窄屏/侧栏被关时 rail 无列表，展开后才可被主区模拟点击）。
验证：build（rspack + doT + terser + assert）通过；注入脚本逻辑按 dsh client-ui-layout/workspace/sidebar/settings-general 0.1.1-rc.2 源码静态核对（会话行/New Session/设置入口 DOM 特征、stopPropagation 阻断链、storage 事件无自环）；DOM 仿真 21 项全过（侧栏捕获 5 项 + 主区分发 7 项 + 端到端 3 项 + 布局回归 3 项 + 折叠强制展开/幂等/重试）；无法实测宿主 UI 渲染——待实装验证项见下。
待实装验证（apps/dsh-hanako/ 部署 + 重启宿主）：宿主侧栏显示 dsh sidebar 完整内容（展开后 180-400px 宽度溢出属可接受边界，记录即可）；主页面 center 全宽无空白轨道；展开后状态跨宿主重启不回归；窄宽下 sidebar 内容可读性；**同步桥实机**——侧栏点会话列表项 → 主区 center 切换落实；侧栏新建会话 → 主区仅创建一个会话（不双开）；侧栏设置入口 → 主区设置面板打开；会话列表加载时序下主区重试生效；localStorage 被宿主 iframe 沙箱禁用时静默降级（侧栏交互回退原生）。
升级注意：无工具契约变化（dsh_run / dsh_update / dsh_session 等参数与回调不变）；新增 cordis 插件由 patch insert + junction 收敛自动挂载，web host 重启即生效；同步桥不引入新存储结构（复用 localStorage key dshana.sync，仅 dshana 参数模式下启用）。

- **v1.0.0-alpha.1**（2026-08-31）：DSHana v2 应用化——声明迁移 v2 + fpFullPanel 全占页（feat/fp-fullpanel，含 v0.26.0 的 cards 迁移基础）。
⑥ **manifestVersion 1 → 2（v2 应用体系）**：contributes.configuration → contributes.settings（properties 包进 schema，v2 settings 结构）；cards 删 type/icon（v2 卡片白名单 Kae 无此二字段，声明会 throw）+ 新增 siteNavEntry: true + fpFullPanel: true；network 字段全在 v2 白名单（Dae）；v2 顶层强制 manifestVersion===2、id 必须等于部署目录名（apps/<id>/）；v1 的 C3t builtin-only 拒绝不适用于 v2 白名单（siteNavEntry/fpFullPanel 社区应用可声明，liliMozi 官方探针 fp-full-probe 验证）。
⑦ **全占页样式 + 版本**：cardForm framed→flush / titlebar solid→translucent（fpFullPanel 全占风格，探针同款）；version 0.25.0 → 1.0.0-alpha.1（v2 时代）；.nvmrc Node 24 → 26。
验证：v2 声明按宿主 0.810.0 bundle 校验逻辑逐字段核对（YBt 顶层 / EBt settings / UBt cards 白名单 / GBt network 白名单）；实装验证 fpFullPanel 生效（独占页面，宿主侧栏被 Full FP 面板取代）。
升级注意：DSHana 由 v1 插件形态过渡到 v2 应用形态（apps/ 部署），功能（入口 cordis 化 / tools/routes 迁移 / embedUrl 嵌 DSH WebUI sidebar）后续落地；宿主侧栏将被 Full FP 面板取代，DSHana 页面切换为整页卡（system 活动条隐掉）。
- **v0.26.0**（unreleased）：DSHana 表面迁移 contributes.cards + functionPanel（feat/cards-app，已并入 v1.0.0-alpha.1）。
① **manifest 迁移**：contributes.page 整块删除 → contributes.cards[]（单卡 id dshana，type webview，route /webui，title DSHana，cardForm framed + titlebar solid + realization page，functionPanel { id: "dshana-panel", label: "DSHana" }）；minAppVersion 0.82.0 → 0.810.0（宿主 dev loop 硬拒 page/widget 表面：PLUGIN_DEV_LEGACY_SURFACE_REJECTED，cards 为唯一新表面；functionPanel.label 实测宿主仅收字符串——渲染侧 label.trim() 对对象直接回退「未命名面板」，pDe 直传但标题带按字符串渲染）。
② **壳页 functionPanel 接入**（src/assets/webui-shell.jinja2）：内置最小 hana 桥（与 @hana/plugin-sdk 同协议 hana.plugin.ui v1：api.url/api.fetch 自动带 pluginSurfaceSession 头 + panel.set/onEvent/onRefresh；宿主注入 window.hana 时优先用宿主实现）；页面加载即 hana.panel.set 推送三段落——status（web host 状态运行中/未就绪/启动中/安装依赖中 + 端口，deltaTone 按状态 success/danger/warning）、actions（手动启动/安装依赖/检测依赖/复制日志路径，activation {kind:"emit"} 回传执行现有 fetch 逻辑，disabled 随状态联动）、meta（日志会话路径只读，有值才推）；refresh: { intervalMs: 5000 } 宿主刷新 tick 经 onRefresh 轻量 health 重读同步面板；bus.ready/pending/diagnostics 事件到达同步更新面板状态；宿主未注入/不支持面板时 pushPanel 吞错静默降级，页面功能回退现状。
③ **浏览器侧 fetch 迁移**：全部 fetch(api + "/webui/...") 改 hana.api.fetch("webui/...")，旧 surfaceHeaders() 手动拼 session 头退役（由 hana.api.fetch 自动带）；startWebHost/installDeps/runVerifyDeps 幂等门禁从「页面按钮存在」改为状态标志（面板无按钮也可调用），页面按钮 UI 反馈保留。
④ **route shell**：body 补 data-surface="card"（对齐 scaffold cards 样例）；/webui 系列端点本身不动（route-backed UI 配套端点，cards 体系下不变）。
⑤ **壳页协议不变**：postMessage ready 握手、hostRequest（clipboard.writeText）、__dshCopy 桥、主题桥（dsh iframe 索取主题 + hana.theme.changed 实时跟随）全部保留。
验证：build（rspack + doT 模板编译 + terser + assert）通过；manifest 按宿主 0.810.0 bundle 校验逻辑静态验证通过（无 page/widget deprecation → dev install 不再被拒；cards 字段全合法；functionPanel id + 字符串 label 合法）；面板段落按宿主渲染器 of/Ye 逐字段核对通过（四状态）。
升级注意：DSHana 从顶部 tab 变为整页卡 + 左侧功能面板；title 由 {zh,en} 对象改纯字符串（宿主渲染器直传 title 属性，对象有 [object Object] 风险，官方样例亦为字符串；如需多语言可改回对象待实测）。
dev loop 实测（plugin.dev.install/diagnostics）未执行：plugin.dev.* 工具不在本会话工具集，且宿主 0.810.0 installPlugin 会 unload+reload 现役插件（其 web host 服务本会话 3080 端口），触发即中断当前运行——已用宿主自身校验逻辑静态验证替代，建议在 Hana 内用 plugin.dev.install 实测。
- **v0.25.0**（unreleased）：超时单位统一为秒 + 可选自定义 nodejs。① **超时单位毫秒 → 秒**（修复工具 schema 文案「秒」与实现毫秒的语义错位）：manifest 配置键 `approvalTimeoutMs` → `approvalTimeoutSec`（默认 30s）、`defaultTimeoutMs` → `defaultTimeoutSec`（默认 1800s=30min），title/description 单位改「秒」并明确说明；工具 `timeout` 参数按秒解释（schema 文案更新为「缺省用插件配置 defaultTimeoutSec」），内部计时保留毫秒（setTimeout 需要毫秒），换算只在边界（工具参数入口秒→毫秒、配置读取秒→毫秒）；lib/config.js `resolveApprovalTimeoutMs` → `resolveApprovalTimeoutSec`（新增 `resolveDefaultTimeoutSec`），优先直读 config.json 新键、旧毫秒键仅在迁移未跑时兜底换算（0=禁用语义保留、正数取整钳 ≥1s，不丢用户配置、不产生单位误解）；**迁移**：src/migrate.js 注册 `timeout-sec` 步骤（config.json global 旧毫秒键存在且新键缺失 → 换算写新键删旧键；新键已存在/旧键不存在 = 零动作，幂等），startWebHostFromPlugin 与 dsh-run doExecute 的 runMigrations steps 追加；卡片链路 URL 参数 timeoutMs 内部契约保持毫秒（展示经 fmtDuration 正确），顺带修复 taskCfg 未带 timeoutMs 导致卡片 URL 从不携带超时预算的断链（恢复态「时间」行超时预算 + 本地倒计时生效）；dsh-run 审批相关注释、README/DESIGN/skills（dsh-run/dsh-approve/dsh-hanako/dsh-install）配置键与单位文案同步。② **可选自定义 nodejs**（修复 macOS 上 Electron 内嵌 node 跑 pnpm 的签名校验失败）：manifest 新增配置项 `nodejsPath`（type string，default ""，title「自定义 Node.js 路径」，description 说明留空用 Electron 自带 node、macOS 签名校验失败时填系统 node 绝对路径如 /opt/homebrew/bin/node）；lib/state.js 静态常量 `ELECTRON_NODE` / `ELECTRON_NODE_ENV` 改为动态解析函数 `resolveNodeExec` / `resolveNodeExecEnv`（每次 spawn 前解析，读 config.json `global.nodejsPath` 优先、配置快照兜底；非空且 existsSync 校验通过 → 自定义路径，路径不存在 warn 降级回退 Electron node 不抛错；`ELECTRON_RUN_AS_NODE=1` 仅 Electron 分支注入——自定义系统 node 不需要）；消费点全量改走新解析：pnpm.js runPnpm、install.js（node 代理脚本 wrapper 内容 + runPnpm env + verifyDepsSmoke spawn，wrapper 与 spawn 同一解析结果）、lifecycle.js（web host spawn + env）；routes/webui.js 过时自检注释同步（实际自检为 t1 依赖 + t2 进程两项）。验证：build（rspack+terser+assert）+ node --check 全量 + ELECTRON_NODE 使用点 grep 无遗漏。升级注意：配置键改名由迁移自动换算（`timeout-sec` 步骤），读取侧保留旧键兜底；nodejsPath 为可选新增配置，不配置行为与旧版一致。
- **v0.24.0**（2026-08-30）：版本迁移统一入口 + 状态收敛（PR #29）。① **src/migrate.js 统一迁移入口**：散落的版本迁移/历史遗留收敛逻辑收敛进注册表式调度器（`MIGRATIONS` 有序数组 + `runMigrations(cfg, opts)` 统一入口，每步幂等、失败不阻断后续）；四条迁移——`archive-old-logs`（旧日志 zstd 压缩归档，原 index.js onload，保留建新会话文件前执行的时序）、`config-schema`（config.json 初始化，幂等不覆盖已有配置）、`junction-converge`（旧名 dsh-hana-* junction 清理 + @dsh-hanako scope 无条件收敛；非链接同名实体预检抛出、ensureWebHost 据此拒绝启动，symlink 环境性失败仍 warn 降级）、`cleanup-update-result`（update-result.json 遗留文件清理）；新增迁移只加一条，调用点（index.js onload / startWebHostFromPlugin / ensureWebHost spawn 前）经统一入口调度。② **单例分组结构化**：平铺状态字段（g.updating / g.updateError / g.depsInstalling / g.depsInstallAt / g.depsInstallError / g.depsInstallLog / g.depsSmoke / g.checking / g.checkResult / g.checkAt）全部废弃，重构为 `g.update / g.deps / g.check` 三组五维（status/result/error/time/log；status 值域 idle/running(installing)/ok/error，各链路定义模块在状态变化点显式维护，单一事实源）；`g.update.log` getter 投影 `g.deps.log`（updateDsh 内部走 installDepsFromPlugin 写同一日志尾环，同源实时）；热更新 getSingleton 逐字段兜底不重置（终态跨热更新可见）。③ **update-result.json 退役**：总线事件化已打通（update.progress/result + 断线排队补发 + 设置页事件缓存），总线不通则更新请求不会发出（设置页是 dsh web host 页面依赖总线；webui update-dsh 按钮已移除非现役），唯一不经总线的入口（Agent 工具 dsh_update）结果全走内存态且从不读文件——文件兜底场景在现役链路不存在；退役落点：updateDsh 删三处文件写（结果走 g.update.result 终态 + 总线事件，status 终态保留）、collectWebDiagnostics 改内存态组合（不再读文件）、buildDepSnapshot 直接取条目 result（同源）、settings 子插件 update-status 删文件读死分支（无缓存即 idle，行为等价）、migrate 清理遗留文件。④ **诊断契约保持**：updateResult.state 值域映射 ok→done / running→updating（webui-shell 按 state==="done" 渲染「更新完成」文案）；更新终态时刻（g.update.time 于 ok/error 刷新）供卡片/诊断 at 显示；web host 重启失败（restartError）同步 g.update.error（诊断 updateError 可见）。验证：build（rspack+terser+assert）+ dsh smoke 37 项（迁移 17 + 状态收敛 20）+ 审查修复 smoke 10 项 + junction 碰撞 4 项。升级注意：无工具契约变化（dsh_run / dsh_update / dsh_install 参数与回调不变）；update-result.json 不再产生，历史遗留由迁移步骤清理；设置页 update-status 无事件缓存时返回 idle。
- **v0.23.3**（2026-08-30）：routes 壳收敛——单壳组合工厂（PR #28）。① **壳 2→1**：dist/routes/ 从 card.js + webui.js 两个转发壳收敛为单一 routes/index.js；src/index.js 的 webuiRoute/cardRoute 具名导出合并为 pluginRoutes 组合工厂（先 webui 后 card，一次挂全部路由），宿主对 default 函数工厂的调用姿势不变。② **构建同步**：build.mjs 壳生成从循环 2 个改为单壳生成（不再随新增路由维护壳清单），rspack.config 注释同步。③ **收益**：新增路由只需在组合工厂加一行（import + 调用），不再动 build.mjs。行为零变化，无升级动作。验证：build BUILD_EXIT=0 + dist/routes/ 仅剩 index.js（terser 后转发 pluginRoutes）+ 动态 import 导出键 default/pluginRoutes 核对。
- **v0.23.2**（2026-08-30）：定位键体系闭环 + 运行期内存态收敛（PR #25 + #26，部分 breaking）。① **非正常终态 minimal 形态**：取消/超时/错误统一走 `deferred:resolve`（宿主对 `deferred:fail` 只呈现 error.message 纯文本、丢定位键，实测确认），新增 `abnormalWakeResult`（wake.js）构造与正常结束同构的定位键 `{ status, rpcId, sessionId }` + 简短 message，status 细分 `cancelled`（DSH_ABORTED）/ `timeout`（DSH_TIMEOUT）/ `failed`；定位键优先级 err.sessionId → loc.sessionId → rpcId。② **提交返回即带 sessionId**：异步返回 content text 直接带 sessionId（jsonl 文件级持久定位键，`await ready` 后已可用；实机确认 details 不进入 Agent 上下文，故放 text——Agent 提交后立即凭 sessionId 取消/取内容，无需反查 list 或等终态回调）。③ **g.ops 运行期协调态收敛**：键从任务 rpcId 改为 sessionId（全链路唯一定位键），条目字段从 `{ rpcId, task, sessionId, approvalPending, pendingApprovals, cancelledRequested }` 收敛为 `{ activeApprovals, cancelledRequested }`（task 原文在 jsonl user/message、sessionId 即键、approvalPending 可由 activeApprovals 派生，三冗余删；审批上下文与取消标记为短命运行期态保留）；`dsh_cancel` 遍历反查改直接 `ops.get(sessionId)` 键控；同 sessionId 多轮 prompt（resume）条目被当轮覆盖（dsh 会话串行，上一轮终态 finally 已删条目）。④ **拒绝路径 rpcId 保留**（CodeRabbit #26 意见）：callUnary / callUnaryBus 的 meta.rpcId 从 reqId 生成时即回传（reject 也保留——reqId 宿主生成、dsh 侧已以此写 jsonl data.source.rpcId）；submitTask catch 从 promptMeta.rpcId 兜底 taskRpcId，resolveReady 失败也带 rpcId（有值时 loc 不再一律 null）；完全无 rpcId（提交未发出）时 text/回调用 `submission-failed` 显式占位（deferredTaskId 仍独立 fallback 保唯一）。**升级注意（breaking）**：`dsh_approve` 参数 `rpcId` → `sessionId`（审批通知 payload 本就双带 rpcId+sessionId，Agent 应答改用 sessionId）。验证：单测 22/22（wake-abnormal +7 断言）+ 回归 110/110 + build + 实机全路径（成功/取消/失败三终态 minimal 形态、提交返回带 sessionId、gops 收敛后取消链路、拒绝路径占位符均实测）。
- **v0.22.1**（unreleased）：**dshana.bus 进程间消息总线**——替代 `POST /child/post` 单向 HTTP 反向信道（PR #22）。① **子插件 @dsh-hanako/bridge**（新，dsh 进程内消息总线服务端）：经 dsh-host-webserver `registerUpgrade` 注册 `/api/dshana.bus` upgrade 路由（握手在 dsh webserver 内完成，不经过宿主路由再分发——bridge 历史教训：feat/bridge-channel 曾做三层通道因宿主 0.769 插件路由再分发丢失 upgrade raw socket/env 不可行，v7 改 HTTP 隧道复杂度爆炸整体 revert（PR #17），本次只做消息总线不做代理）；handler 内零依赖手写 RFC6455（`ws-lib.js`：握手/帧编解码/分片重组/256KB 单帧上限/连接级错误处理），首帧 hello（`{ channel:"hello", payload:{ token } }`）与 config 注入的 `busToken` 比对（patch 模板 `{{BUS_TOKEN}}` 注入），单连接语义（新连接顶掉旧连接），provide `dshanaBus`（emit/on/status）。② **宿主侧 `src/lib/bus.js`**（新，WS 客户端）：Node 22+ 全局 WebSocket 连 `ws://127.0.0.1:<webPort>/api/dshana.bus`，open 后首帧发 hello，断线指数退避重连（1s→封顶 30s）+ 应用级心跳 `bus.ping`/`bus.pong`，单例 `g.bus`；web host 就绪点生成 `busToken`（每次 spawn 随机 16 字节 hex 存 g.busToken）→ 渲染 patch 注入 → 就绪后 `connectBus`，closeProcess/卸载 `closeBus`。③ **更新链路迁移**：设置页 request-update 改经总线发 `update.request`（不再 fetch hostApi `/child/post`；bus 未就绪返回「消息总线未连接」），宿主收到 → `g.updateDsh`（复用现有能力层）→ 完成后总线回投 `update.result { state, version?, error? }`；并发防护复用 `g.updating`。④ **/child/post 退役**：删除 `routes/child.js` + index.js 注册 + build.mjs 壳，删除 `readChildHostApi`（lifecycle.js），patch 模板删除 hostApi 段，settings 删除 hostApi 依赖。⑤ **降级**：bus 连接失败不阻断 dsh 启动（request-update 报错即可，check-version 走 dsh 侧直查不受影响）；更新结果以 `update-result.json` 为准（update-status 轮询读）。⑥ **文档同步**：DESIGN / dsh-hanako / dsh-update SKILL 全面更新。
- **v0.23.1**（2026-08-30）：总线 RPC 收敛——指令面总线化（PR #24）。① **Unary RPC 走总线**：session.create / prompt / selectModel / cancel + respond 审批应答改经 dshana.bus（新通道 `rpc.request`/`rpc.result`，reqId 配对，复用 update 链路模式）；bridge 子插件新增**总线 RPC 翻译器**——收总线请求后本机自环 `fetch 127.0.0.1:<port>/api/<method>`（dsh mux 零改动）回投结果，异常必回投防宿主 pending 挂死。② **总线优先、HTTP 兜底**：总线未连接时降级回退 HTTP 直连（appendLog 记录），dsh_run/dsh_cancel/dsh_approve 可用性不受总线故障影响。③ **保留直连**：`/api/events.mux` WS 事件流（流式 chunk/审批帧/终态，高吞吐不适合事件通道）与 `/api/host.describe` 就绪探测（connectBus 之前的鸡生蛋）继续 HTTP。④ CodeRabbit 修复：respond 降级路径信封语义（client-response 信封原样直发，不再经 client-request 再包层）。安全：不设 method 白名单（本机信任模型，总线只是换入口）。验证：dsh 单测 25/25 + respond 信封 8/8 + 回归 112/112 + build。升级注意：工具面（dsh_run/dsh_cancel/dsh_approve）对外行为不变（内部传输变更，HTTP 兜底）。
- **v0.23.0**（2026-08-30）：进程间消息总线 dshana.bus + 通信全面收敛（PR #22 + #23，breaking）。① **消息总线**：子插件 `@dsh-hanako/bridge` 在 dsh webserver 注册 upgrade 路由 `/api/dshana.bus`（复用 dsh 端口 3080，零依赖手写 RFC6455 帧编解码/分片重组/心跳/单连接顶替，provide dshanaBus：emit/on/status/getConfig）；宿主侧 `src/lib/bus.js` WS 客户端（指数退避重连 + 心跳 + 单例 g.dshanaBus）。更新链路迁移：设置页 request-update → 总线 update.request（带 reqId 等宿主 update.ack 确认）→ 宿主 updateDsh → 终态单发 update.result（断线排队补发不丢）。② **/child/post 退役**：删除 routes/child.js、readChildHostApi、hostApi 配置与 loopbackToken 凭据链路。③ **免鉴权**：总线与 mux、/api/session.* 同级本机信任（hello 身份宣告，无 token——第二轮 CodeRabbit 凭据建议因设计冲突跳过）。④ **tpl 纯化**：config 下发（dshPkgDir/dataDir 经总线）+ log 转发（logger → 总线 → 宿主统一落盘，断连期有界缓冲补发）+ 静态 `dsh-plugin/dsh-hanako.patch.yml`（纯 insert 加载声明），`renderPatchTpl`/generated/全部占位符删除。⑤ **轮询事件化**：壳页就绪改 bus 事件驱动（postMessage ready 挂载 iframe，30s 兑底），`/webui/health` 收缩为纯诊断端点；update-status 2s 轮询 → update.progress/result 事件；provider refresh 走总线（`/api/hana-provider.refresh` 退役）。⑥ **CodeRabbit 三轮 21 处修复**：g.bus 命名冲突（宿主总线 vs 客户端总线）、update.result 排队、channel 隔离（ch: 前缀防 EventEmitter 保留事件）、分片协议（continuation 帧重组/分片中新帧 1002/结束帧超限 1009）、监听器异常隔离、emit 排队语义、日志失败保留重试、refresh 时序握手、SSE/轮询清理、updateEventCache 清空、单终态等。验证：回归 112/112 + 三轮单测 40/40 + build。**升级注意（breaking）**：Agent 回调仍为 `{status,rpcId,sessionId}`；总线协议替代全部进程间 HTTP 碎片；`/api/dshana.bus` 免鉴权仅限本机。
- **v0.22.0**（2026-08-30）：工具面收敛 + 回调契约收口 + 权限收敛（PR #20 + #21，breaking）。① **dsh_session 统一会话工具**（list/get 两模式）取缔 `dsh_ops` + `dsh_search`：list 继承清单能力（projcache，limit 1~100 默认 10，最新在前）；get 新增——凭 sessionId 直取会话元数据 + 最终结论 summary（读会话 jsonl 的 zstd 多帧容器，Node 内置 `node:zlib` `zstdDecompressSync` 按帧 magic 0xFD2FB528 逐帧解压，零依赖；定位先按 projcache cwd 猜编码目录、失败遍历兜底；找不到/解压失败返回 `{ok:false,error}` 不抛栈）。② **callbackMode 三档收口固定 minimal**：manifest 配置项删除，所有 dsh_run 回调只带定位键 `{ status, rpcId, sessionId }`（不再冗余 id 字段——id 与 sessionId 同值重复，收敛唯一定位键；同步模式 details.dsh 同步删除 id），不生成摘要（buildSummary 及其常量连带删除）；取内容统一走 dsh_session get。③ **删除 session-query-sqlite patch 注入**：dsh 恢复默认 `openAt: never` 禁用全文搜索，不再有全局全文搜索入口（session-query 服务无调用方授权，禁用即收敛）；dsh_session search 模式随之移除。④ **权限模型**：sessionId 即访问凭证——会话存在性与内容的唯一事实源在 dsh-home（sessions/ + session_projcache.json），拿得到 id 即可 get/resume，无需所有权表（config.json sessions 注册表方案已撤销，registerSession/readSessionRegistry 删除）。⑤ **安全**：get 模式 sessionId 精确 UUID 结构校验（`session-` + 8-4-4-4-12 hex），拒绝畸形值/路径穿越，防读取 sessions/ 之外的外部 session.jsonl.zstd；resume 保持 session.list 存在性检查。**升级注意（breaking）**：`dsh_ops`/`dsh_search` 工具已删除，Agent 调用方改用 `dsh_session`（list/get）；callbackMode 配置项已删除，回调固定 `{status,rpcId,sessionId}`。
- **v0.21.3**（2026-08-30）：callbackMode 直读 config.json 即时生效（PR #19）——minimal 回传不生效根因：dsh_run 的 callbackMode 此前只读宿主注入的配置快照 `ctx.config.callbackMode`，设置界面保存 / Agent 直改 config.json 后快照未刷新，回调仍走默认 summary。修复：`tools/lib/config.js` 新增 `resolveCallbackMode(cfg)`，优先直读 dataDir/config.json 的 `global.callbackMode`（单一事实源，与 defaultCwd/approvalTimeoutMs 同款兜底），无则回退快照，非法值回 summary；`dsh-run.js` 改用该函数解析，minimal 分支逻辑不变。验证：node --check + 5 场景单测全 PASS。
- **v0.20.1**（2026-08-28）：macOS arm64 崩溃兜底——web host spawn 注入 `--expose-internals`（PR #12）。HMR 服务需要 Node 内部 ESM loader，上游 drop 该 flag 后改走原生 addon 兜底（`require('node-addon-require-builtin')`），但该 addon 在 macOS arm64 上加载失败（`Unsupported/no-getter (arm64 …)`），致 `dsh web` boot 崩溃（deepseek-harness 讨论 #113）。显式注入 flag 切到 `require('internal/modules/esm/loader')` 直连路径（Hana 内置 node 24.15，v2 loader），绕开崩溃 addon；Windows x64 等其余平台走直连同样成立、行为不变。arm64 修复需 Apple Silicon 实机验证（已验证 Windows 不回归）。
- **v0.20.0**（2026-08-28）：opId 完全废除——运行期协调键统一为任务 rpcId。① **卡片链路清理**：卡片定位收敛为 sessionId+rpcId 唯一定位键——`/card/op`、`/ops/status`、`/ops/output` 不再接受 opId 参数（校验收紧为两者必填，与 `/ops/stream` 对齐），模板删 `data-op`，card.js 删 opId 变量 / `opQuery` 无效参数 / 合成值检查，`readOp` 签名去 opId；dsh_run 卡片 route 不再带 opId，ready 失败的 opId-only 降级路径随 op Map 退役移除。② **运行期协调键统一为任务 rpcId**：g.ops 条目键、toolCallCache / approvalTimers 键、deferred taskId、审批通知 payload、工具契约字段全面从 opId 迁移到任务 rpcId（session.prompt 提交产生的 RPC id，与 jsonl user/message 的 data.source.rpcId 同值，运行期协调键与数据定位键合一）；`createOpEntry` 时序迁移到 prompt 提交成功后（审批/取消均发生在 prompt 后，时序无碍）；审批对象帧 rpcId 改名为 `respondRpcId`（server-request 信封自己的 RPC id，仅 `/api/respond` client-response 路由用，与任务 rpcId 物理隔离防混淆）；`nextOpId` 删除。③ **工具契约 breaking**：`dsh_approve` 参数 opId → rpcId（`required: ["rpcId", "approvalId"]`）；`dsh_cancel` 删除 opId 参数（`required: ["sessionId"]`，按 sessionId 遍历 g.ops 定位条目标记取消）；`dsh_run` 返回 `details.dsh.opId` → `rpcId`、后台回调 payload opId → rpcId、content 文案同步；deferred taskId = 任务 rpcId（提交失败兜底 `dsh-run::<ts>` 唯一键）。④ **文档同步**：dsh-run / dsh-approve / dsh-cancel / dsh-hanako 四份 SKILL + DESIGN.md 全面更新（参数表 / 返回结构 / 通知 payload / taskId 格式 / 错误语义），dsh-ops / dsh-search 顺带清理历史 opId 表述，全仓库仅 CHANGELOG 历史记录保留旧词。升级注意：Agent 调用方需按新契约传参（rpcId / sessionId）。
- **v0.19.0**（2026-08-28）：pnpm 运行时引导摘除 + 检查更新去 pnpm + UI 收敛 + 文案规范化。① **pnpm 运行时引导**：zip 不再内置 `node_modules/pnpm`（19MB → 0.1MB），devDependencies 移除 pnpm；新增 `src/tools/lib/pnpm.js`（`ensurePnpm` 幂等引导：固定版本 11.24.0 + 双文件 `pnpm.mjs`/`worker.js` 逐文件 sha256 校验 + unpkg/jsdelivr 双源 + 原子落位 + 缓存自愈 + 并发单例 + `tryHostChannel` 宿主通道探测位；实测证伪单文件假设——`pnpm add` 导入阶段经 `new Worker` 加载 `worker.js`，缺它静默退出 1，故双文件引导）；版本与校验同源静态配置（`PNPM_BOOTSTRAP`，杜绝动态版本×静态校验不同步）；`packageManager` 保留（CI 工具链声明，与运行时解耦）；消费方收敛至 install 两处（部署 + verify 检查）。② **检查更新去 pnpm**：settings 侧与宿主 `lib/check.js` 的远端版本查询从 spawn pnpm view 改 **HTTP 直查 npm registry**（fetch `/latest` 的 JSON version，pnpm view 语义等价；官方源失败重试 npmmirror，15s 超时）；patch 模板删除 `{{NPM_CLI_PATH}}`/`{{ELECTRON_NODE}}` 注入（settings config 只剩 dshPkgDir/dataDir），lifecycle `renderPatchTpl` 改回同步，**web host 启动零 pnpm 依赖**（原 onload 预热方案随之取消）。③ **verifyDeps 叠加 pnpm 引导检查**：`verifyDepsSmoke` 与 dsh 冒烟并行调 `ensurePnpm`（缺失自动重下自愈，finally 等收尾定格结果），独立子项 `pnpmReady/pnpmVersion/pnpmError`（不进 t1 判定），deps 卡片展示「pnpm 引导」行，settings 卡片不展示；`installDepsFromPlugin` 加「pnpm 引导就绪」里程碑日志。④ **UI 收敛**：deps 卡片移除「检查更新」「更新 DSH」按钮（版本管理归设置页「检查与更新 DSH」卡片 + dsh_update 工具），版本行简化为仅当前版本；`/webui/check-update`、`/webui/update-dsh` 路由与能力层保留。⑤ **文案规范化**：UI/工具描述/错误信息中 DSH 专有名词缩写统一大写、标题首字母大写（16 文件 +79/-79）。
- **v0.18.1**（2026-08-26）：dsh 插件组收敛 `@dsh-hanako` scope + 版本检查 dsh 侧直查 + 更新桥接轮询化 + provider 幽灵依赖修复。① **插件组收敛**：dsh-plugin 下五个 cordis 插件（`dsh-hana-theme/provider/settings/logger/clipboard`）收敛为 `@dsh-hanako/*`（目录/包名/cordis.name/patch 模板/junction 全量同步，git mv 保留历史）；`ensureCordisJunctions` 启动时清理旧名遗留 junction（`dsh-hana-default-model`（v0.13.0 改名前的历史残留）/ `dsh-hana-proxy` 等），无条件收敛到当前命名；日志 src（theme/provider/settings）、设置页路由前缀（`/api/hana-settings.*`）、slot id（`dshana-settings`）、注入服务名（`hanaLogger`）保持稳定（dsh UI 内部契约不破坏）。② **版本检查改 dsh 侧直查**（修复「检查更新永久 pending」）：设置页检查此前走 `update-request.json` → 宿主 `resources.watch` 桥接 → `checkDshUpdate` → `check-result.json` 读回，watch 链路依赖宿主 resources 注入 + bus 事件订阅，不可靠（实测写入后宿主无感知、check-result 永不更新、前端永久 pending）；改为设置页后端直接 spawn 宿主 `electronNode` + `pnpm.cjs` 执行 `pnpm view @deepseek-ai/dsh version`（官方源失败自动重试 npmmirror，15s 超时），结果直接响应，`check-result.json` 落盘删除（`checkDshUpdate` 保留内存缓存供工具/标签页）。③ **更新桥接轮询化**：宿主侧 `ensureUpdateWatch` 从 `resources.watch` + `bus.subscribe` 改为 **5s 轮询** `update-request.json`（at 去重，`g.updateWatchLastAt`；`onBridgeRequestChanged` 只留 `requested` 分支），不依赖宿主 resources/bus 注入；`update-result.json` 保留（更新进度必需）。④ **provider 幽灵依赖修复**（修复 `no adapter serves provider deepseek`）：pnpm 严格结构下 `dsh-pkg/node_modules` 顶层只链接直接声明依赖，间接依赖（`@earendil-works/pi-ai`）不在顶层——npm 扁平时代是幽灵依赖碰巧可用，pnpm 下 provider 插件 `loadDeps` 经 `dshPkgDir` 解析失败 → 按设计降级空转（插件已挂载但供应商不注册、宿主 push `/api/hana-provider.refresh` 404、dsh 报 no adapter）；修复：`loadDeps` 依赖基座改为 `profiles/node_modules`（dsh junction farm 全量依赖视图，与 dsh 运行时一致）优先，`dshPkgDir` 保留回退。⑤ **CodeRabbit 配置**：`.coderabbit.yaml` 禁用 `docstrings` pre-merge check（JS/ESM 项目按 Python 语义统计纯误报，注释质量由代码评审保证）。
- **v0.18.0**（2026-08-26）：供应链安全防线 + 依赖管理 npm → pnpm 迁移 + 实装测试修复闭环。① **供应链防线**（PR #7）：新增 `.github/CODEOWNERS`（`* @ProjectNyaser` 亲审供应链命门）、`dependabot.yml`（npm weekly，major 单独 PR 不自动合并）、`ci.yml`（pnpm frozen install + build + `pnpm audit --audit-level=high` 门禁，catch npm 12.0.2 漏洞类回归）；actions 升 v5；CI 显式 `pnpm/action-setup@v4`（钉 packageManager 版本，修复 setup-node 只配缓存不装 pnpm 的 `Unable to locate executable file: pnpm`）。② **npm → pnpm 迁移**：内置 pnpm（devDependencies `pnpm` + pack 打包 `node_modules/pnpm` 供部署），dsh 部署改 pnpm——`installDepsFromPlugin` 不再复制插件根 package.json（其 devDeps 是 rspack 构建树，而 pnpm 11 的 add 命令无 `--omit` 旗标、报 `Unknown option: 'omit'`），改为写最小 package.json（无 devDeps，pnpm add 天然只装 dsh 运行时树）+ 复制 `pnpm-workspace.yaml`（`allowBuilds` 白名单放行 dsh 树 build scripts，取代 pnpm 11 不再读取的 package.json `allowScripts`/`onlyBuiltDependencies`）+ `pnpm add @deepseek-ai/dsh --loglevel=http`（官方源失败自动重试 npmmirror）；部署前停 web host（closeProcess，Windows 文件锁前提：清理要删被运行中进程加载的 koffi/node-pty 原生模块）+ 升级兼容清理（清 `package-lock.json` / `pnpm-lock.yaml` / 扁平 `node_modules`，防旧 npm 残留与 pnpm `.pnpm` 结构混装破坏 cordis 依赖解析）；版本检查 `npm view` → `pnpm view`（内置 pnpm.cjs，修复 zip 不再含 `node_modules/npm` 后 `Cannot find module npm-cli.js`）；web host launcher 加 `--no-open`（dsh 启动不再弹默认浏览器）；CI/release 全链路 pnpm（frozen install + cache + audit）。③ **实装测试修复闭环**：更新确认改两段式（宿主沙箱 iframe 无 `allow-modals`，`window.confirm` 被浏览器忽略，设置页与标签页同步改造）；webui-shell 更新/安装**自动退诊断页显示进度**（health 恒带 diagnostics、就绪态低频兜底轮询、updating/installing busy 判定防跳变、更新完成后自动切回并强制刷新（时间戳 bust）、诊断页按钮事件委托无条件绑定）；**rc 版本截断回归根治**（v0.16.0 修过 compareSemver 的 rc 比较，但 verifyDepsSmoke 提取版本的正则 `^\s*(\d+\.\d+\.\d+)` 丢弃 `-rc.x` 后缀，`0.1.1-rc.1` 被截成 `0.1.1`，与 `0.1.1-rc.2` 比较时被当正式版判「已是最新」；修复为捕获完整预发布后缀）；pack.mjs 交付清单加 `pnpm-workspace.yaml`（install.js 部署依赖，缺失即 throw）；工具描述/诊断文案 npm i → pnpm add 全面同步；文档 DESIGN / dsh-hanako / dsh-install / dsh-update SKILL 同步 pnpm 部署流程。依赖基线：master 从 npm 体系整体切 pnpm（`pnpm-lock.yaml` 替换 `package-lock.json`），`pnpm audit` 零漏洞。
- **v0.17.1**（2026-08-26）：两项增量——① **dsh 内部偏好实时化**：dsh-hana-theme 注入脚本经 `/api/events.host` WebSocket 订阅 dsh 自身的 `settings/document-updated` 变更广播（`ui-theme` namespace），用户切 dsh 偏好（`system`/`light`/`dark`）实时生效，无需重开标签页（此前仅在加载时读一次 `settings.describe`）。实现要点：首次用 `EventSource` 订阅误读为 SSE，实测 `/api/events.host` 实为 WebSocket（普通 GET 返回 426，README 明确不保留 SSE 回退），改为同源 WebSocket 下行，帧 `server-request` 信封 `payload` 内取 `host/remote-event`，`args[0]==="ui-theme"` 时经 `refreshPref()` 回读并 `applyOrRemove()`；断线指数退避重连（1s→封顶 30s，至多 10 次）、失败静默退回「加载时读一次」。② **兼容 dsh 0.1.1 launcher 标志顺序**：`src/lifecycle.js` `ensureWebHost` 把 `--patch` 移到 `--profile` 之前（dsh 0.1.x 将 `--profile` 之后的参数透传给 web app，`--patch` 在后被拒 `unknown option`，致 web host 启动失败）；对当前 0.1.0-rc.7 两种顺序解析一致、无副作用。文档跟注：DESIGN / SKILL 的「dsh 偏好仍重开标签页」改为「实时生效」。
- **v0.17.0**（2026-08-26）：主题实时跟随——壳页接宿主 `hana.theme.changed` 广播，宿主切 Hana 主题后**免重开标签页**实时跟随。① **订阅而非快照**：此前壳页仅在挂载时读一次主题快照，宿主切主题后仍推旧值、需重开标签页生效；本次 message 监听新增 `hana.theme.changed` 分支（限定 `e.source===window.parent`），收到后按新 cssUrl 更新壳页主题 `<link>`（精确锚定 href 含 `/theme.css`，避免误换其它样式表）；② **明暗同步**：同步 `data-hana-theme` 与 `meta color-scheme`，让诊断区/跨源 iframe 的 `prefers-color-scheme` 跟随宿主明暗；③ **时序兜底**：onload/onerror/300ms 兜底后经 `readThemeVars()` 重读新变量 `sendThemeTo` 重推内层 dsh iframe（dsh-hana-theme 幂等）；保留 1.5s 定时重推与 `dshHanaThemeRequest` 应答作 fallback，互不冲突。④ **文档跟注**：DESIGN「主题跟随」生效时机与 SKILL「已知限制」同步改为「宿主切主题实时，dsh 偏好仍重开标签页」；仅改壳页模板 `src/assets/webui-shell.jinja2`（+61/-1），未动宿主 / dsh-hana-theme 内层 / next 分支。
- **v0.16.0**（2026-08-21）：版本比较修复——`parseSemver`/`compareSemver` 原先只把预发布后缀 `-rc.x` 视作布尔 pre，三段相同时直接判相等，导致 `0.1.0-rc.6` 到 `0.1.0-rc.7` 被误判为无更新。修复：parseSemver 的 pre 提升为结构 `{ kind: 'rc', num: N }`，非数字预发布后缀（`-beta`/`-alpha` 等）按 `{ kind: 'pre', num: 0 }` 视为更旧；compareSemver 三段相同且同为预发布时比较 num 序号（`rc.6 < rc.7` 判 -1）；保持既有语义（正式版 > 同版本预发布、同版本无后缀相等返回 0、解析不合法返回 null 且比较返回 0）。同步修正 dsh-plugin/dsh-hana-settings 与 src/tools/lib/install.js 同款复制。零依赖，未引入 semver 库。
- **v0.15.3**（2026-08-21）：架构重构——单 bundle 收敛 + registerTool 声明式注册 + 独立 rspack.config + asset/source 内联；① **源码组织收敛**：根目录 index.js、tools/（7 工具 + lib×6）、app/（lifecycle.js、card.js/css）、routes/ 全量迁入 src/（仅保留会被编译的源码），scripts/ 保持根目录不参与编译（build/pack/version/rspack.config/minify-loader/template-loader）；② **构建收敛为单 bundle**：多入口（index + tools×7 各出 bundle）改单入口 src/index.js → dist/index.js（生命周期 + 7 工具 + lib + lifecycle + 内联前端资源一个文件），manifest 移除 contributes.tools 改 index onload 经 ctx.registerTool 动态注册 7 工具（宿主自动加 pluginId_ 前缀，registerTool 返回清理函数交 this.register 卸载逆序自动清理），dist/routes/*.js 变壳（import { webuiRoute } from "../index.js" 转发），manifest entry 仍指向 index.js；③ **独立 rspack.config.mjs**：node target + ESM 输出（library.type=module——default 插件类 + webuiRoute/cardRoute 具名导出），externalsPresets.node 保持零运行时依赖，asset/source 内联前端资源（卡 js/css 经 minify-loader terser/clean-css 压缩后内联）；④ **模板构建期编译**：webui/card 的 HTML 模板从 JS 模板字面量（${}）分离为 src/assets/*.jinja2（.jinja2 扩展名避免 HTML 静态检查器把 {{= expression }} 当非法标记误报），构建期 template-loader 用 doT（devDependencies，dependencies 恒空）编译为自包含渲染函数（产物不含 doT 运行时，彻底移除运行时 renderTpl/new Function 模板求值）；⑤ **构建后额外 terser**：rspack（swc）压缩 + import.meta.url 静态化替换（staticUrlToMeta 递归收集 src/ 下 .js）之后对 dist 整体再走一轮 terser（index.js -0.2%、routes 壳 -16%），assertNoStaticFileUrl 在 terser 后兜底校验（产物零构建机路径泄漏）；零依赖纪律保持（dependencies 恒空，dist bundle 只 import node:*，无模板库引用），pack 适配（不复制 app/routes，dist 无 tools/ 目录）。
- **v0.15.0**（2026-08-21）：web host 生命周期分离 + 异步启动——① **生命周期独立模块**：dsh-run.js 瘦身为纯任务提交流程模块（2716 → 1269 行），不再承担 web host 生命周期；生命周期（ensureWebHost / closeProcess / collectWebDiagnostics / updateDsh / provider 热跟随 watch / DSH 检查更新桥接 watch / provider 路由 / config 日志）整体迁入 **app/lifecycle.js**（1460 行），职责从提交链路中抽离；② **分发形态不变**：app/lifecycle.js 只被 dsh-run 静态 import，rspack 内联进 dist bundle 保持 ?t= 整包刷新能力（staticUrlToMeta 递归收集 .js + assertNoStaticFileUrl 兜底，产物不残留构建机路径）；routes/webui.js 与 index.js 不静态 import，仍经 globalThis 单例挂载调用（模块缓存纪律不变）；③ **启动改异步**：index.js onload 不再轮询式 await startWebHost（旧：最多 5s 等单例挂载 + 60s 端口就绪等待，可能拖住宿主启动），改为单例挂载轮询 ≤1s 后 fire-and-forget 触发启动，onload 立即返回不阻塞加载；启动失败记 g.webLastError / g.webLastLogPath，首次工具调用（dsh_run execute 内 ensureWebHost）或 POST /webui/start 手动启动仍可靠重试；④ **单例契约保持**：g.closeProcess / g.collectDiagnostics / g.updateDsh / g.startWebHost / g.installDeps / g.verifyDeps / g.checkDshUpdate 等字段全量保留，路由/工具消费面不变。
- **v0.14.0**（2026-08-20）：provider 配置数据流重构——子进程不再直接读取 models.json / provider-catalog.json，改为宿主进程组装 route 目录后经 HTTP push 下发；pushProviderRefresh 改为有界重试（退避表 300~3000ms）覆盖路由注册空窗期，改用全局 fetch 直连回环地址避开网络代理服务拦截；启动就绪点主动推一次 routes，路由组装结果缓存到 g.latestProviderRoutes。
- **v0.13.4**（2026-08-20）：剪贴板桥收尾修复。① **复制成功反馈断链**：壳页面桥（routes/webui.js）回执原本走 `e.source.postMessage`（window 消息），而 dsh 前端桥（dsh-hana-clipboard 的 `__dshCopyBridge`）等待的是随请求传来的 **MessageChannel 端口**（port1.onmessage）——通道不匹配，宿主写剪贴板成功但反馈永远回不到 dsh UI（按钮不显示「已复制」，2.5s 后超时显示失败）。修复：壳页面回执优先走 `e.ports[0].postMessage`（端口回执，与桥端匹配），无 port 时 fallback window postMessage；② **manifest 版本同步**：v0.13.1~v0.13.3 发版时只 bump package.json，manifest.json version 停在 0.13.0，发布包内 manifest 与 tag 不一致，随本版同步为 0.13.4（顺带修正）；③ CHANGELOG 同步。
- **v0.13.3**（2026-08-20）：**剪贴板桥正规化 + 回滚 v0.13.2 无效方案**。① 新增 dsh 插件 **dsh-hana-clipboard**：经 dsh-host-webserver `tapIndex` 扩展点向 dsh Web UI 注入桥脚本，嵌入场景（DSHana 标签页 iframe）下覆盖 `navigator.clipboard.writeText`——原生调用被宿主插件 iframe 跨源 Permissions-Policy 继承链拦截（NotAllowedError）时自动转接宿主 capability `clipboard.writeText`（manifest `ui.hostCapabilities` 白名单）→ 宿主主窗口上下文执行（不受插件 iframe 权限链限制）→ MessageChannel 回执；顶层直接浏览 dsh UI（浏览器开 `<port>`）时桥静默（isEmbedded 守卫），`writeText` 保持原生零影响。正规 dsh 插件机制（tapIndex + patch.yml insert + junction 挂载），不修改任何 node_modules 文件，dsh 升级后照常生效；② **回滚 v0.13.2 iframe 权限声明**：实测证明跨源继承链（宿主插件 iframe 与主窗口跨源且无 allow 属性）下内层 iframe 自身声明 sandbox/allow 无法生效，恢复 dsh-frame 裸嵌（见下条）；③ 壳页面桥（routes/webui.js：mini host SDK hostRequest + `__dshCopy` 监听转发）——dsh 前端桥消息的中转层；④ manifest 新增 `ui.hostCapabilities: ["clipboard.writeText"]`；dsh-run.js junction 列表 + dsh-hana-clipboard。
- **v0.13.2**（2026-08-20）：~~修复 dsh WebUI 跨源 iframe 权限被宿主沙箱连坐~~ **（v0.13.3 已回滚：无效方案）**——曾尝试给内层 dsh-frame 显式声明 `sandbox`（含 `allow-downloads allow-modals allow-pointer-lock`）+ `allow="clipboard-read; clipboard-write; fullscreen; autoplay"`，期望覆盖宿主沙箱限制恢复复制。实测（CDP 跨源结构复现 + 用户环境 featurePolicy 探针）证明：宿主插件 iframe 与宿主主窗口跨源（file:///app://）且无 allow 属性时，Permissions-Policy 继承链整体 deny，**内层 iframe 自身声明无法覆盖父级 inherited deny**（allow 属性只能在本层生效，不能逆链解锁），`clipboard-write` 仍为 false，复制依旧失败。**该方案整体回滚**（dsh-frame 恢复裸嵌，见 v0.13.3），剪贴板问题的最终解法为 dsh-hana-clipboard 插件宿主能力桥。注：sandbox token 部分（allow-downloads 等）本可保留（显式声明替换继承语义下对 dsh UI 下载/弹窗有效），经决策一并回滚保持干净。
- **v0.13.1**（2026-08-20）：修复 CI 出包构建机路径泄漏——v0.13.0 lib 提取（tools/lib/*.js 内联进 bundle）后，rspack 把内联模块里的 `import.meta.url` 静态化为构建机绝对路径，build.mjs 后处理仅收集 entry URL 做回替换，`lib/state.js` 的 `file:///home/runner/...` 残留进产物。分发到 Windows 后模块顶层 `fileURLToPath` 直接抛错 → dsh-run.js 加载失败，能力层（installDeps/verifyDeps/updateDsh/checkDshUpdate/closeProcess）挂载语句全部未执行，dsh_install/dsh_update 统一报「插件工具模块未加载（dsh 能力层不可用）」。修复：① staticUrlToMeta 收集范围从仅 entry 扩展为全部会被内联的源码（递归收集 ROOT 下 .js，排除 dist/node_modules/releases/_tmp/.git）；② 新增构建后强制校验 `assertNoStaticFileUrl`——产物残留任何带引号 file:// 字面量即构建失败（CI 出包必检，回归防护）。替换后 import.meta.url 指向 bundle 自身位置（dist/tools/xxx.js），向上找 manifest.json 一步即达插件根，定位语义不变
- **v0.13.0**（2026-08-20）：dsh-hana-default-model 改名升级为 **dsh-hana-settings**（DSHana 设置）——目录/包名/slot id（default-model → dshana-settings）/路由前缀（`/api/hana-default-model.*` → `/api/hana-settings.*`）/日志 src（default-model → settings）全量改名，分页新增 **DSH 版本块**（`@deepseek-ai/dsh` 版本检查与更新）；**检查/更新收敛为宿主能力层单一事实源**（`tools/dsh-run.js` 挂单例 `g.checkDshUpdate` / `g.updateDsh`，三消费面共用：① 新 Agent 工具 **dsh_update**（action=check/update，update 默认异步后台执行 + 完成回调，wait=true 同步；更新会重启 web host、正在执行的任务中断）② DSHana 标签页（新路由 `GET /webui/check-update`、`POST /webui/update-dsh` + deps 卡片版本行「当前/最新/可更新」与「检查更新」「更新 DSH」按钮）③ dsh 设置页「DSHana 设置」分页（本地版本 dsh 侧直读 dsh-pkg package.json 零延迟；远端版本经 `update-request.json { state:'check-requested' }` 桥接请求 → 宿主 `ensureUpdateWatch`（`ctx.resources.watch`）→ `checkDshUpdate`（本地版本复用 verifyDepsSmoke 缓存/直读 + 远端 `npm view`，官方源失败重试 npmmirror，zero-dep semver 比较）→ 结果写 `check-result.json` 读回，宿主检查未完成返回 pending 由前端轮询；更新闭环 = 「更新到最新」写 `{ state:'requested' }` → 宿主 `updateDsh`（停 web host → npm i latest → 起 web host → 读新版本）→ 写 `update-result.json { state: done|error, version?, error?, at }` → 前端每 2s 轮询 update-status 直到 done/error）；并发防护 `g.checking` / `g.updating`（进行中重复请求跳过 + 检查 5s 时间窗防 npm view 重复跑）；v1 草案中「dsh 侧 spawn npm view 直查远端」逻辑废弃（检查统一走宿主能力层）；patch 模板段4 config 注入 `dshPkgDir`/`npmCliPath`/`electronNode`/`dataDir`（占位符 `{{DSH_PKG_DIR}}`/`{{NPM_CLI_PATH}}`/`{{ELECTRON_NODE}}`/`{{DATA_DIR}}`），junction 列表改名，renderPatchTpl 新增 3 占位符；manifest 新增 `./tools/dsh-update.js`，build.mjs 入口 +1；日志 src 枚举 default-model → settings（宿主根 index.js / dsh-hana-logger / DESIGN 同步）。**v3：共用模块提取 + dsh_install 工具 + 安装/升级卡片**——① **tools/lib/ 提取**：`lib/state.js`（getSingleton 单例 + 环境常量 + manifestDefaults + `g.depTasks` 默认）、`lib/install.js`（resolveDshPkgDir / installDepsFromPlugin / verifyDepsSmoke / semver 比较 / readDshInstalledVersion）、`lib/check.js`（checkDshUpdate）；dsh-run.js 瘦身（移除已迁定义改 import，web host 生命周期 + updateDsh 保留，单例挂载不变）；rspack 入口静态 import lib（bundle 内联，?t= 重载即刷新），routes/index 仍经 globalThis 单例；② **新 Agent 工具 dsh_install**（action=install 默认：npm i @deepseek-ai/dsh 到 dsh-pkg，registry 兜底 + 自动运行级重验 + autoStart（web host 未起自动拉起，失败不阻断）；action=verify：运行级冒烟只读；默认异步 + 完成回调 + 安装卡片，wait=true 同步；`g.depsInstalling` 防并发）；manifest 注册 + build.mjs 入口 7→8；③ **安装/升级卡片可视化**：`g.depTasks` 登记表（taskId → kind/state/log/at/result）+ 新路由 `GET /card/dep?taskId=`（iframe 卡片页，data-kind="dep"）+ `GET /ops/dep-stream`（SSE：首帧快照 + 每 1s 推 log 增量，终态关闭，30s 心跳）+ `GET /ops/dep-status`（兜底 JSON）；数据源 = g.depTasks + g.depsInstallLog（npm 实时日志尾部）+ update-result.json（update 合并）；app/card.js `initDepCard` 分支渲染（标题 DSH 安装/升级 + 状态徽标 + 日志实时滚动 + 完成结果），按 data-kind 分流不触碰任务卡片；dsh_update 异步 update 接入同款升级卡片（depTasks 登记 + card URL）。**v3.1：日志流规范化 + npm 输出实时落盘**——① installDepsFromPlugin 内部统一日志通道 `emitLog(s, src)`：同一份文本进内存尾环 `g.depsInstallLog`（≤`DEPS_LOG_CAP`=8000，旧实现 npm 流式累积不设上限内存无界增长）+ 会话日志文件（src=npm 记 npm i 原始输出、src=hana 记里程碑，均**实时逐 chunk 写**，取代旧「命令完成后一次性 `g.appendLog("npm", out)`」）；npm 输出行规范化 `\r\n`/裸 `\r` → `\n`（进度帧折行不再糊成一坨）；② 会话日志行规范化（index.js `appendLogLine` / dsh-run.js `appendLog` 兜底同规范）：多行 chunk 逐行加 `[ts] [src]` 前缀（旧实现整块只加一次前缀，后续行无前缀不合规范）、空行丢弃、chunk 内共用一个时间戳（单次 append 性能不变）；日志 src 枚举 + npm（索引注释同步）；③ 故障诊断上下文补齐——verifyDepsSmoke 记 `[依赖验证]` 开始/通过/失败（cliBin + 退出码/错误尾）、checkDshUpdate 记 `[版本检查]` 开始/完成（本地版本来源 + 远端查询结果/错误，原只 console.log 不进会话日志）；④ 里程碑统一 `[依赖安装]` 前缀进会话日志（src=hana），修复 "Created proxy node at" 缺空格
- **v0.12.0**（2026-08-18）：Relicensed from MIT to MPL-2.0.
- **v0.11.5**（2026-08-17）：nodePath 配置移除——插件改用宿主 electron 进程自身的 Node 运行时（`process.execPath` + `ELECTRON_RUN_AS_NODE=1`）启动 web host，无需用户单独安装 Node.js。自检从三项减为两项（t1=dsh 依赖，t2=DSH 进程），移除 t1 Node 配置相关路由（verify-node/adopt-node）与对应文档，t1→t2 门禁机制保留并重编号
- **v0.11.4**（2026-08-17）：文档同步更新——README / SKILL / 代码注释对齐最新状态（移除过期版本号提示、修正行数字段、清理注释重复）
- **v0.11.2–v0.11.3**（2026-08-17）：稳定性迭代（卡片高度定稿、运行中快照防缓存、jsonl 恢复边界加固）
- **v0.11.1**（2026-08-16）：文档同步修正——README 任务反馈卡片章节更新为 SSE 推送渲染（baseline + /ops/stream，插件零状态、jsonl 唯一事实源、重启后恢复），审批流程改运行期协调条目表述；dsh-ops/dsh-run SKILL 的运行态与完整输出来源改 jsonl 恢复语义（不再重启即清）；dsh-cancel SKILL 补 sessionId 来源实操示例（异步回调/卡片 URL/dsh_ops/opId 反查四途径）；明确 opId 定位（宿主卡片契约 + Agent 指代 + 运行期协调条目键，不参与数据定位）
- **v0.11.0**（2026-08-16）：卡片架构大版本——**SSE 推送 + 零状态插件**。① 卡片链路从 HTTP 轮询改为 SSE 服务端推送：新增 `/ops/stream`（先推 baseline = jsonl 恢复快照，再按 sessionId 转发 DSH mux 实时事件），卡片 EventSource 本地拼文本节流渲染、收 blocks/usage、turn/end 定终态，轮询退役（`/ops/status`、`/ops/output` 降级为纯 jsonl 兜底）；② **op Map 退役**：插件不再存任务快照（status/output/summary），session.jsonl 为唯一事实源，运行中归一化（重建窗口无 turn/end 如实显示 running，不再误报完成），审批/取消靠运行期协调条目（终态即删）；③ `dsh_cancel` 支持 sessionId 直接取消；④ 本地超时倒计时（URL timeoutMs + startedAt）；⑤ 顺带合入终态瘦身（op.output 清空留预览）、URL 携带 timeoutMs（恢复态时间行有预算）、词元缺失不显示（cache/thinking 无源不兜 0）
- **v0.10.45**（2026-08-16）：终态瘦身 + URL 携带 timeoutMs——① **内存降级**：任务终态后 op.output 全文清空（只留 1KB outputPreview + outputLength），完整输出按需从会话 jsonl 恢复（recovered 路径 + LRU 缓存），op Map 50 条内存从 MB 级降到 ~150KB；② **URL 带 timeoutMs**：卡片 URL 追加 timeoutMs（会话日志无该配置项），恢复态「时间」行能显示超时预算（readOp 在快照无值时用 URL 值覆盖）
- **v0.10.44**（2026-08-16）：词元缺失不显示——累计器初始化不再把 cacheReadTokens/reasoningTokens 兜成 0（字段恒存在导致 fmtUsage 误显示 0）：缺失字段不初始化，API 未返回（如 deepseek-v4-flash 不报 cache/reasoning 明细）时卡片不显示对应项，真 0 仍显示 0；dsh-run 累计与 routes mergeUsage 同步修正
- **v0.10.43**（2026-08-16）：详情区细节——label 宽度 56px→auto（自适应内容，值区更宽），详情区左右 margin 8px（相对卡片边缩进）
- **v0.10.42**（2026-08-16）：任务行展开保留换行——任务描述含换行符时展开后平铺（HTML 文本节点默认折叠换行为空格），修复：折叠态 `white-space: nowrap`（单行截断不受换行干扰），展开态 `white-space: pre-line`（保留任务描述换行）
- **v0.10.41**（2026-08-16）：滚动摘要预览 1KB——outputPreview 1MB 改 1KB（1024 字符，滚动摘要显示量更足；1MB 轮询体过重）
- **v0.10.40**（2026-08-16）：滚动摘要预览放宽——outputPreview 300 字符 → 1MB（≈不截断，dsh 输出远小于此），滚动摘要直接显示完整输出（不再因 300 字符尾部截取显得量少、文本内容时卡片偏矮）
- **v0.10.39**（2026-08-16）：恢复 body margin——margin:0 视觉贴边太紧，恢复默认 8px，body max-height 600→584（584 + margin 16 = html 600 锁宿主上限不变）
- **v0.10.38**（2026-08-16）：body margin 重置——实测 html 616px = body 600px + 默认 margin 8px×2，body 规则补 `margin: 0`（浏览器默认 8px 未重置导致 documentElement 溢出 16px），html 与 body 对齐 600
- **v0.10.37**（2026-08-16）：纯 CSS 按需拉长——移除 JS 动态限高，改 flex 布局：body `flex column + max-height:600 + box-sizing:border-box`，中间层（#dsh-root/.dsh）参与收缩链（flex:0 1 auto + min-height:0），固定元素 flex-shrink:0，摘要区 `flex:0 1 auto + min-height:0 + overflow-y:auto`；内容短 body 贴合、长时顶满 600、摘要区收缩到剩余空间滚动（本地验证：短 282/中 600/长 600）
- **v0.10.36**（2026-08-16）：摘要区按需拉长——移除写死限高（339），改为 JS 动态计算：渲染后实测固定部分（头部/详情/按钮/meta）高度，摘要区 max-height = 600 - 固定部分 - 2px，body 总高锁 600；内容不足自然贴合、超出内部滚动（视图切换后重新 fit）
- **v0.10.35**（2026-08-16）：运行时完整输出结构化——dsh-run 事件循环同步收集 assistant/message 的 blocks（text/reasoning/tool-call），终态 op.output 改用 `dsh-blocks-v1::JSON`（reasoning 可折叠，与恢复态同构；回调/返回值保持 chunk 流文本）；readOp 的 outputLength 按 text 块口径；卡片终态后重拉一次完整输出（运行时拉的是 chunk 文本，终态刷新为结构化）
- **v0.10.34**（2026-08-16）：切换按钮文案分态——运行中显示「滚动摘要」（实时预览），终态显示「最终结论」，完整输出视图仍「完整输出」
- **v0.10.33**（2026-08-16）：运行态限定滚动行为——滚动条隐藏与固定滚底仅作用于运行中实时预览（.dsh-summary.live 类，render 按 running 切换）：运行中隐藏滚动条并固定滚底跟随最新输出；终态摘要与完整输出视图恢复正常滚动（显示滚动条、自由滚动）
- **v0.10.32**（2026-08-16）：摘要区限高 + 滚底跟随——回退固定高方案：摘要区恢复 max-height 339px（内容短贴合、长则内部滚动），滚动条隐藏（scrollbar-width:none / ::-webkit-scrollbar 隐藏），内容更新与视图切换后自动滚到底部（跟随最新输出）
- **v0.10.31**（2026-08-16）：摘要区高度固定化——限高值（339）保留但由 max-height 改为固定 height：内容不足时留白、超出时内部滚动，卡片高度恒定 ≈600（不再随 md 渲染内容高度波动），所有任务视觉统一
- **v0.10.30**（2026-08-16）：宿主偏移补偿——实测 v0.447.4 宿主 card iframe = 上报 - 16（上报 584→iframe 568，600→584），上报余量 0→+16 补回宿主减去的偏移，让 iframe 贴合 html（html 599.5 → 上报 616 → iframe 600）
- **v0.10.29**（2026-08-16）：限高修正——v0.10.28 限高 361 实测 html 621.5px 超出（实际头部 260.5px 而非估算 238.5px），回调至 339px（600-260.5=339.5），内容重新顶满 600 不超出
- **v0.10.28**（2026-08-16）：高度顶满与 iframe 贴合——按实测（html 553.5px / iframe 568px）调整：摘要区限高 315→361px（内容顶满宿主 600px 上限），上报余量 +30→0（iframe 贴近 html 高度，去除多余空间）
- **v0.10.27**（2026-08-16）：详情区行合并——「开始」与「超时」合并为「时间」行（格式 `<开始时间> / <超时预算>`，如 `20:11:42 / 30m`；无超时或恢复态只显示开始时间），详情区固定 6 行（任务/目录/模型/时间/词元/状态），高度更稳定
- **v0.10.26**（2026-08-16）：恢复快照补齐模型行——从事件窗口内首个 request/header 事件提取实际模型配置（data.header.config 的 provider/model/reasoningEffort，如 deepseek/deepseek-v4-flash/high），恢复卡片详情区不再缺「模型」行（超时行为配置项，日志无源，保持不显示）
- **v0.10.25**（2026-08-16）：恢复输出结构化（reasoning 折叠）——恢复快照的完整输出不再拼 chunk，改为从 assistant/message 的 content blocks 按序收集：`dsh-blocks-v1::JSON`（text 块 markdown 渲染 / reasoning 块渲染为 `<details>` 可折叠「思考过程」/ tool-call 块小字显示工具名）；卡片渲染器 renderBody 识别该前缀（运行时 chunk 流输出不受影响，仍走 mdToHtml）；预览与长度按 text 块文本口径；实测首个任务恢复：reasoning 23 / text 129 / tool-call 187 块，文本 121KB，摘要 2.1KB 分离
- **v0.10.24**（2026-08-16）：恢复输出口径修正——摘要与完整输出对齐运行时语义：① **完整输出** = 事件窗口内 assistant/chunk 的文本增量拼接（与 dsh-run 运行时 textFromChunk 同口径：delta.text ?? block.text ?? text，含 agent 中间叙述；chunk 流无文本时回退 assistant/message 拼接）；② **摘要** = 最后一个 finish（reason.kind==stop，工具循环结束的最终 LLM 调用）之后的 assistant/message 文本（最终回答）；修复恢复卡片摘要与完整输出相同的问题（实测：181KB 过程输出 vs 2.1KB 最终报告）
- **v0.10.23**（2026-08-16）：卡片持久化架构——op 数据从会话日志可恢复（零映射文件）——① **rpcId 关联**：callUnary 暴露 rpcId，session.prompt 调用记录 op.rpcId（与会话 jsonl 的 user/message 事件 data.source.rpcId 同值）；② **URL 推迟生成**：submitTask 加 ready 信号（session.create + prompt 提交后 resolve），卡片 URL 推迟到此后携带 sessionId+rpcId，重启后精确恢复；③ **jsonl 恢复路径**：/ops/status 与 /ops/output 支持 sessionId+rpcId，内存 op Map miss 时从会话日志（追加式多帧 zstd，逐帧解压）按 user/message（data.source.kind==user + rpcId 匹配）定位，取事件窗口重建 op 快照（task/耗时/输出/词元累计/turn-end 原因），缓存 LRU 20 条避免重复解压；④ 旧 opId-only 卡片兼容（内存路径不变）
- **v0.10.22**（2026-08-16）：词元统计修正——卡片词元从「最后一轮 LLM 调用用量」（覆盖式收集，多轮任务严重偏小）改为**任务维度累计**（每轮 assistant/message 的 disjoint usage 求和：未缓存输入/输出/缓存读取/推理），与 dsh 会话投影 tokenUsage.totals 口径对齐（实测：Docker 任务卡片原显示 out 318，实际累计 out 7935 / cache 22528）；resume 复用会话时统计本次任务的消耗，而非会话历史累计
- **v0.10.21**（2026-08-16）：详情区行序调整 + 耗时实时化——① **行序重排**：任务 → 目录 → 模型 → 开始 → 超时 → 词元 → 状态（状态移至末行）；② **运行中耗时实时计算**：头部时长不再用 op 快照值（发出时定格），改用 startedAt 实时推算（now - startedAt），且轮询渲染时即使内容未变化也刷新 dur（每秒走动，终态后回落到准确总耗时）
- **v0.10.20**（2026-08-16）：移除冗余 cwd 行——头部下方的「cwd: …」行与详情区「目录」行重复，删除该行并把高度让给摘要区（限高 293→315px），卡片总高不变、内容区更大
- **v0.10.19**（2026-08-16）：卡片高度与预览收敛——① **限高放宽**：摘要区 280→293px，按实测差距（卡片 586.5px → 600px）放宽，整体贴满宿主上限且不触发 clamp；② **尾部预览截取减半**：运行中摘要的尾部预览从 600 字符减到 300 字符（outputPreview slice 调整），滚动场景下摘要更紧凑
- **v0.10.18**（2026-08-16）：卡片高度稳定化——① **词元行常显**：详情区词元行始终显示（无 usage 时显示「—」），详情区行数结构稳定，卡片高度不再随词元有无波动（解决有词元时偏短、无词元时偏长的观感差）；② **限高下调**：摘要区 max-height 300→280px，配合词元行常显与 +30px 上报余量，有/无词元时卡片高度趋于一致且不超出宿主 600px 上限
- **v0.10.17**（2026-08-16）：agentPreset 默认补齐——标题的 agent 预设未显式传时，从 settings.yaml 的 `agent-presets.default` 读取补齐（如 code），标题显示「DSHana · code」，与 dsh 默认预设一致；显式传参仍优先（op 快照与 session.create 均用补齐后值）
- **v0.10.16**（2026-08-16）：修复三处——① **按钮文案修正**：显示摘要时按钮「摘要」、完整输出时「完整输出」（v0.10.15 写反）；② **切换按钮始终显示**：不再等输出超 600 字符才出现，摘要区上方恒有切换按钮；③ **默认模型解析修复**：readDshDefaultModel 缩进出块判断 `<=2` 误伤 2 空格标准缩进（YAML 子项），改为仅无缩进/非键行才出块——修复「模型」行（provider / model / effort）显示不出来
- **v0.10.15**（2026-08-16）：卡片信息增强与高度定稿——① **标题带 agent 预设**：头部显示「DSHana · <agentPreset>」（未指定时仅 DSHana）；② **新增模型行**：详情区显示 provider / model / reasoningEffort（显式参数优先，缺省从 dsh 默认补齐；op 快照与 /ops/status 新增 agentPreset/reasoningEffort/provider/model 字段）；③ **切换按钮文案简化**：显示摘要时按钮「摘要」、完整输出时「完整输出」（去字符数与箭头）；④ **高度定稿**：上报余量 +30px，摘要区限高 300px，默认状态整体不超出宿主 600px 上限
- **v0.10.14**（2026-08-16）：卡片细节调整——① **切换按钮上移**：完整输出/摘要切换按钮置于摘要区上方（右对齐，margin-bottom 6px）；② **限高回调 + 高度补偿**：摘要区限高 335→320px，高度上报余量 +3→+18px 补回 15px，整体卡片高度保持不变；③ **词元中文化**：详情区标签「Token」改「词元」（值格式 in/out/cache/thinking 保持英文术语）
- **v0.10.13**（2026-08-16）：卡片展示重构——① **标题固定 DSHana**：头部标题行固定显示插件名，任务文本改由详情区任务行承载（单行截断 + 点击展开）；② **摘要/完整输出共用容器**：同一容器按钮切换内容（摘要 ⇄ 完整输出），完整输出仍懒加载缓存、切换即时渲染；③ **摘要区限高 320→335px**：整体卡片高度 +15px，内容区显示更多（总高仍稳在宿主限制内）
- **v0.10.12**（2026-08-16）：卡片背景与高度余量修正——① **背景挪到 body**：`background: var(--tool-bg)` 从 .dsh 容器移到 iframe body（覆盖整块卡片区域，宿主注入变量时生效，未定义回退透明）；② **高度上报 +3px**：scrollHeight 取整后加 3px 余量，抵消宿主 iframe 边框/取整差异，避免内容贴底时出现轻微滚动条
- **v0.10.11**（2026-08-16）：卡片交互与高度治理——① **任务值展开状态持久化**：展开状态存 JS 变量（taskOpen），轮询重建 DOM 后保持展开，不再自动收起（上版 class 存 DOM 的缺陷）；② **摘要区限高内部滚动**：max-height 320px + overflow-y auto，卡片总高稳在宿主 iframe 高度限制内，内容超长时摘要区内滚动、iframe 不再出现滚动条（与完整输出区同款）；③ **卡片整体背景**：`.dsh` 容器加 `background: var(--tool-bg)`（宿主未定义时回退透明）
- **v0.10.10**（2026-08-16）：卡片高度自适应修复 + 任务值单行可展开——① **高度上报增强**：`document.fonts.ready` 字体就绪后补报 + `ResizeObserver` 跟随 body 内容尺寸变化自动重报（防抖 60ms），根治宿主字体异步加载导致卡片刚出现时高度偏小、带滚动条的问题（终态后轮询停止、高度定格在偏小值的时序缺陷）；② **任务值单行可展开**：详情区「任务」行默认单行截断（-webkit-line-clamp 1），点击展开/收起全文（hover accent 色提示可点击）
- **v0.10.9**（2026-08-16）：卡片摘要/输出区排版重做——代码块（纸面分层 + 0.5px 细边框 + 4px 圆角 + 12px 字号）、表格（去四边网格改表头下划线 + 行间细分隔线 + 末行去线 + tabular-nums 数字对齐）、引用（2px accent 边条 + 淡蓝便签底 + 斜体）、标题字重 600→500 + h2 accent 边条、列表圆点着 accent 色、hr 改细线、加粗 500+accent、内联代码加细边框（host 主题变量 + fallback，跨主题可用）
- **v0.10.8**（2026-08-16）：三大功能块 + 统一日志服务——① **宿主 push 配置跟随重构**（方案 A）：宿主侧 `ctx.resources.watch` 感知 models.json / provider-catalog.json 变化，bus 派发 `resource.changed` → 防抖 300ms → POST dsh web host `/api/hana-provider.refresh`，dsh 侧插件重读配置 `handle.replace()` 原子更新，替代 dsh 进程内 fs.watch（manifest 加 `resource.watch` / `network.fetch` capabilities + allowedHosts）；② **统一日志体系定稿**：时间戳会话文件 `<YYYYMMDD-HHmmss-SSS>.log`（文件名=应用层创建时刻，毫秒唯一，不受 NTFS CreationTime 怪癖影响），旧日志 zstd 压缩全部保留（Node 内置 `node:zlib`，标准格式 magic 28b52ffd，零依赖），诊断面板显示当前会话日志路径，**无 latest.log 指针**；③ **junction 无条件重建**（lstatSync 实体判断不跟随，悬空/指向旧路径一律删旧建新，修旧版本悬空 junction 启动报错）；④ **dsh-hana-logger 统一日志服务**——新内嵌 cordis 插件 `provide 'hanaLogger'`（服务名驼峰：不能用 'logger'——cordis 内置 LoggerService 冲突；不能连字符——注入属性按服务名原样挂载），theme/provider/default-model 三插件 `inject ['hanaLogger']` 统一写日志，删除各自内联 appendFileSync 实现（单一实现，src 前缀不变）；logPath 仅 logger 段注入，消费方零配置；patch 模板五段、junction 四包；日志文案精简（无首行冗余、压缩行去括号）。含 0.10.7（assets/ → 根级 dsh-plugin/ 结构重构，git mv 保留历史；移除 session-query 静态回退）、0.10.6（config/ → assets/patches/ 归并）、0.10.5（静态资产 terser/clean-css 压缩，zip 185→168KB）、0.10.4（三插件统一包名注册 hana-theme / hana-provider / hana-default-model 修复列表显示不一致）中间迭代
- **v0.10.3**（2026-08-16）：dsh_ops 翻新——不再用 ops.jsonl 记录历史，直接解析 dsh 会话持久化缓存 `<dataDir>/dsh-home/storages/session_projcache.json`；新增 `limit` 参数（默认 10，范围 1~100）只返回最新几条；移除 `status` 过滤（新数据源无状态语义），主键由 opId 改为 sessionId，返回 sessionId/title/cwd/createdAt/lastPromptAt/usage/turns/steps/llmMs；同步移除 dsh-run 的 ops.jsonl 落盘/恢复机制（-180 行，运行期 op 快照保留，卡片/审批/取消不受影响；旧 ops.jsonl 数据文件保留不删）。主 SKILL 精简：删除主题跟随/默认模型配置冗余章节（使用语义并入首次安装配置与已知限制，约 -2100 字符），description 移除「发版」残留；SKILL 路由表补 POST /webui/adopt-node 与 README 对齐（v0.11.5 已移除）
- **v0.10.2**（2026-08-16）：config.json 自动初始化——插件初始化（onload 拉起 web host / 首次工具调用）时若 `<数据目录>/config.json` 不存在，按 manifest 默认值自动生成（`ensureConfigJson`：`{ schemaVersion: 1, global: { ...manifestDefaults }, agents: {}, sessions: {} }`，幂等不覆盖已有内容、原子写 + 失败静默）；全新安装免「先保存一次」引导，装完即可在设置界面看到默认值
- **v0.10.1**（2026-08-16）：配置项收敛与工具技能体系——**移除 hostProvider 配置项**（modelsPath/catalogPath 不再配置，宿主 provider 路径直接探测：`HANA_HOME`（宿主进程注入）→ 插件安装形态 `<宿主数据目录>/plugins/<pluginId>` 上溯 → `homedir()/.hanako`，存在性验证命中；清除 manifest/文档中的开发机路径泄漏）；**移除 agentPreset 配置项**（不显式传时不传 `session.create` 的 agentPreset 字段，用 dsh Web UI 默认 agent 预设，reasoningEffort 同理）；`defaultTimeoutMs` 默认 600000 → 1800000（10 分钟对编码任务偏短，默认 30 分钟）；**新增五个独立工具 SKILL**（dsh-run/dsh-approve/dsh-cancel/dsh-ops/dsh-search，从源码 tools/\*.js 核对参数语义/返回结构/错误码/审批通道/副作用），索引 skill 工具速查挂接，README 工具章节精简为列举 + SKILL 链接
- **v0.9.5**（2026-08-15）：彻底移除插件自身「填 API Key」「手动设置模型」回退路径——删除 `apiKey` / `model` / `reasoningEffort`（全局）配置与 `.credentials.yaml` / config.json 读取链；`hostProvider` 恒开跟随宿主（无「关闭」选项，不再有官方 API 回退路线），凭据直读宿主 `provider-catalog.json`；任务模型默认 = dsh 默认（settings.yaml `agent-default-model`），`dsh_run` 工具参数 `provider`/`model`/`reasoningEffort` 显式覆盖（显式时 selectModel，dsh 会把所选模型写回全局默认）；**新增 dsh-hana-default-model 插件——dsh 设置页「默认模型」配置块**（Provider/模型/思考强度三级联动（切 provider 自动选中首个模型 + 默认思考强度，打开分页回显当前默认），选项 = llm.models 权威列表全部 provider，保存即写 agent-default-model 生效，补上 settings 页缺失的该段配置 UI）；**默认模型分页正规化升级**——前端由 tapIndex DOM 注入改为 dsh 前端 `settings.section` slot 注册原生渲染（client.js + package.json `dsh.client` 声明，patch 段4 包名注册 + `$DSH_HOME/profiles/node_modules` junction 解析，导航自动投影分页、点击切换全走原生 React）；诊断不再显示 apiKey。含 0.9.4（dsh-hana-provider 宿主 provider 直通定稿：dsh_run 加入 provider/model/reasoningEffort 工具参数与 selectModel 显式分支、readDshDefaultModel、错误透传）中间迭代
- **v0.9.3**（2026-08-15）：t1 候选列表体验完善（空候选提示「未检测到本机可用 Node.js」+ 去用户可见文案写死版本号与版本后缀）+ 配置生效文案统一（t1/t2/t3 依赖相关「重启 Hana」全部改为「立即生效/完成后自动验证」，与页面自愈能力对齐）；含 0.9.1（t1 fix 文案去重启）、0.9.2（候选探测扩展 nvm-windows NVM_HOME / volta VOLTA_HOME，FNM_DIR 多版本遍历刻意不实现）中间迭代
- **v0.9.0**（2026-08-15）：大版本收尾（0.8.3 以来全量迭代，CI 三段式发版，GitHub Release pre-release）——**连接失败自检与自愈体系**（web host 未就绪时标签页自检 t1 node 配置 / t2 dsh 依赖 / t3 进程状态 + 操作按钮进卡片）：t2 依赖运行级验证（`node cliBin --version` 冒烟抓假就绪）+ 页面自装（POST /webui/install-deps，npm ci --omit=dev 部署 dsh-pkg，实时进度）+ 手动启动（POST /webui/start）+ 运行级检测（GET /webui/verify-deps / verify-node，进页自动一次 + 手动按钮）；门禁链 t1→t2→t3（依赖/Node 未通过时下游按钮禁用）；webLastExit 持久退出记录；诊断区主题化（CSS 变量跟随宿主）；配置生效分层（检测层实时 / 启动层现读 config.json / 仅旧进程存活需杀进程）；SKILL 瘦身（15323→7362 字符，移除「构建与打包」章节与构建触发词，定位为排障手册）
- **v0.8.3**（2026-08-15）：风险审查处置——清理 credentialsPath 死路径（manifest 未声明该设置项，错误提示与注释不再引用；凭据文件兜底收敛为 dsh-home/.credentials.yaml → ~/.dsh/.credentials.yaml）；主题插件 file:// URL 加 encodeURI（路径含空格/特殊字符时不再生成非法 URL，主题加载更稳）
- **v0.8.2**（2026-08-15）：文档翻新——README 无版本痕迹化 + 主题跟随章节 + 名称/ID 规范；SKILL 部署运维版（主题排错、config.json 缺失引导）
- **v0.8.1**（2026-08-15）：主题跟随宿主完整落地——`dsh-hana-theme` cordis 插件（tapIndex 注入 index 响应，安装目录 dsh-plugin file:// 加载，patch 模板渲染注册），宿主声明取色（壳桥 getComputedStyle 读 theme.css 16 变量，随宿主更新、新增主题零适配），72 个 `--dsw-alias-*/--dsw-specific-*` token 映射（视觉主表面 + 遮罩 + 滚动条，功能性颜色保留原生），preference 边界（system 才覆盖，light/dark 原生，加载时读一次 settings.describe），覆盖写 body 层 !important（压 dsh presenter 的 body inline）。生效：切偏好/切主题重开标签页
- **v0.8.0**（2026-08-15）：DSHana 标签页主题跟随基础版——壳页面按 hana-theme 映射 color-scheme（跨源 iframe 继承 prefers-color-scheme，dsh system 跟随宿主明暗）；升级清依赖事故处置（npm ci 部署 dsh-pkg，升级不丢依赖）；SKILL 排错表新增升级场景
- **v0.7.5**（2026-08-14）：版本号迭代（功能见 v0.7.x 早期迭代）
- **v0.7.4**（2026-08-14）：版本号迭代（功能见 v0.7.x 早期迭代）
